# 📄 링크 인 바이오 서비스 화면 설계서 (AI 전달용)

## 🧩 1. 서비스 개요

- 서비스명: (미정)
- 목적: 여러 사용자가 자신의 링크를 모아 공유하는 **Link-in-Bio 플랫폼**
- URL 구조:
  👉 `https://yourdomain.com/{username}`

---

## 👤 2. 사용자 구조

- 회원가입 / 로그인 필요
- 사용자별:
  - 프로필 페이지 생성
  - 링크 관리
  - 클릭 통계 확인

---

## 🎨 3. 디자인 컨셉

- 키워드: **단순함 / 직관적 / 세련됨 / 현대적**
- 스타일: **애플 스타일 (미니멀 + 여백 중심)**
- UI 특징:
  - 넓은 여백
  - 라운드 버튼
  - 그림자 최소화
  - 부드러운 애니메이션

---

## 📱 4. 메인 프로필 페이지 (Public Page)

### 🔹 레이아웃 구조 (세로형)

```
[프로필 이미지]
[닉네임]
[한 줄 소개]

[링크 버튼 1]
[링크 버튼 2]
[링크 버튼 3]
...
```

---

### 🔹 상단 프로필 영역

- 프로필 이미지 (원형)
- 닉네임: `스파이더`
- 한 줄 소개: `누구나 만들 수 있다`

---

### 🔹 링크 버튼 리스트 (핵심 UI)

- 버튼형 UI (Linktree 스타일)
- 세로 나열
- 버튼 구성:
  - 아이콘 (좌측)
  - 텍스트 (중앙)
  - 전체 클릭 영역

#### 기본 링크 유형

- 오픈 카카오톡
- 유튜브
- 스레드
- 링크드인

---

### 🔹 버튼 스타일

- 둥근 모서리 (radius: 16~20px)
- hover 시 살짝 확대
- 클릭 시 subtle animation

---

### 🔹 테마 기능

- 상단 우측 토글 버튼

#### 모드 3가지:

1. ☀️ Light
2. 🌙 Dark
3. ⚙️ Auto (OS 따라감)

---

## ⚙️ 5. 사용자 마이페이지 (관리 페이지)

### 🔹 기능 구성

#### 1. 프로필 설정

- 프로필 이미지 업로드
- 닉네임 수정
- 한 줄 소개 수정

---

#### 2. 링크 관리

- 링크 추가 / 수정 / 삭제
- 링크 유형 선택 (아이콘 자동 매칭)

👉 드래그 앤 드롭으로 순서 변경

---

#### 3. 클릭 통계

- 각 링크별 클릭 수
- 총 클릭 수

---

#### 4. 테마 설정

- 라이트 / 다크 / 자동 선택

---

## 📩 6. 문의하기 기능 (모달)

### 🔹 트리거

- "문의하기" 버튼 클릭 시 모달 팝업

---

### 🔹 모달 UI

```
[문의하기]

이름 입력
이메일 입력

[보내기 버튼]
```

---

### 🔹 입력 필드

- 이름 (필수)
- 이메일 (필수)

---

### 🔹 동작

- 제출 시:
  - Notion DB에 저장
  - 성공 메시지 표시
  - 모달 닫기

---

## 🔗 7. Notion API 연동 설계

### 📌 1. 준비

1. Notion Integration 생성
   👉 [https://www.notion.so/my-integrations](https://www.notion.so/my-integrations)

2. 권한:

- Insert content
- Read content

---

### 📌 2. DB 생성

Notion에서 DB 생성 (예: "문의 관리")

컬럼:

- Name (title)
- Email (text)
- Created At (date)

---

### 📌 3. DB 공유

👉 생성한 Integration을 DB에 초대 (Share)

---

### 📌 4. API 호출 예시 (Node.js)

```javascript
const fetch = require("node-fetch");

const NOTION_API_KEY = "YOUR_API_KEY";
const DATABASE_ID = "YOUR_DATABASE_ID";

async function sendToNotion(name, email) {
  const response = await fetch("https://api.notion.com/v1/pages", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${NOTION_API_KEY}`,
      "Content-Type": "application/json",
      "Notion-Version": "2022-06-28",
    },
    body: JSON.stringify({
      parent: { database_id: DATABASE_ID },
      properties: {
        Name: {
          title: [
            {
              text: { content: name },
            },
          ],
        },
        Email: {
          rich_text: [
            {
              text: { content: email },
            },
          ],
        },
      },
    }),
  });

  return await response.json();
}
```

---

## 🧠 8. 상태 정의 (AI 개발용 핵심)

### 상태 목록

- `theme`: light | dark | auto
- `links`: 배열
- `modalOpen`: boolean
- `formState`: { name, email }
- `userProfile`: { image, nickname, bio }

---

## 🔄 9. 사용자 흐름

### 🔹 일반 사용자

1. 링크 페이지 접속
2. 버튼 클릭 → 외부 이동
3. 문의하기 → 폼 제출

---

### 🔹 로그인 사용자

1. 로그인
2. 마이페이지 이동
3. 링크 추가
4. 순서 변경
5. 통계 확인

---

## 🧱 10. 컴포넌트 구조

- ProfileHeader
- LinkButton
- LinkList
- ThemeToggle
- ContactModal
- Dashboard
- DragList

---

## 🚀 11. AI 개발용 프롬프트 (핵심)

👉 이거 그대로 AI에게 넣으면 됨

```
Create a responsive Link-in-Bio web app with the following features:

- Multi-user system with profile pages at /{username}
- Apple-style minimal UI with clean spacing
- Profile section with image, nickname, bio
- Vertical list of link buttons (Linktree style)
- Theme toggle: light / dark / auto (OS-based)
- Contact modal with name + email input
- Save form data to Notion API
- Dashboard for users:
  - Edit profile
  - Add/remove/reorder links (drag & drop)
  - View click analytics
- Use smooth animations and rounded UI
- Mobile-first design
```

---
